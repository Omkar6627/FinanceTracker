import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.omkar.financetracker',
  appName: 'FinanceTracker',
  webDir: 'dist/frontend',
};

export default config;
